const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'../../..');const read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('native checkout captures one passenger per selected seat and does not swallow payment failure',()=>{const s=read('BusGoMobile/src/app/booking/checkout.tsx');assert.match(s,/seats\.map\(emptyPassenger\)/);assert.match(s,/passengers:passengers\.map/);assert.doesNotMatch(s,/demoComplete\(id\)\}catch\{\}/)});
test('native booking management exposes cancellation refund reschedule review and support',()=>{const api=read('BusGoMobile/src/lib/api.ts');for(const x of ['refundStatus','cancellationQuote','rescheduleOptions','rescheduleQuote','confirmReschedule','review','createSupport'])assert.match(api,new RegExp(x));for(const p of ['BusGoMobile/src/app/booking/manage.tsx','BusGoMobile/src/app/booking/reschedule.tsx','BusGoMobile/src/app/booking/review.tsx','BusGoMobile/src/app/support.tsx'])assert.ok(fs.existsSync(path.join(root,p)),p)});
test('native search exposes useful bus type departure and rating filters',()=>{const s=read('BusGoMobile/src/app/search/index.tsx');for(const x of ['SLEEPER','SEATER','MORNING','AFTERNOON','EVENING','rating'])assert.match(s,new RegExp(x))});
test('broken guest-booking route is no longer referenced',()=>{for(const p of ['frontend/src/pages/Customer/CustomerLoginPage.tsx','frontend/src/pages/Customer/CustomerBookingsPage.tsx'])assert.doesNotMatch(read(p),/\/guest-booking/)});
