import { Alert, StyleSheet, Text, View } from 'react-native'; import { router } from 'expo-router'; import { Screen } from '@/components/Screen'; import { Card, Muted, PrimaryButton, Title } from '@/components/UI'; import { useAuth } from '@/context/AuthContext'; import { colors } from '@/theme/colors';
export default function Profile(){const{user,signOut}=useAuth();const logout=()=>Alert.alert('Sign out','Sign out of BusGo?', [{text:'Cancel',style:'cancel'},{text:'Sign out',style:'destructive',onPress:async()=>{await signOut();router.replace('/login')}}]);return <Screen><Title>Profile</Title><Card><View style={s.avatar}><Text style={s.initial}>{(user?.name||user?.mobile||'B').slice(0,1).toUpperCase()}</Text></View><Text style={s.name}>{user?.name||'BusGo customer'}</Text><Muted>{user?.mobile||''}</Muted>{user?.email?<Muted>{user.email}</Muted>:null}</Card><Card><Text style={s.item}>🔒 Secure account</Text><Text style={s.item}>🎫 Booking history</Text><Text style={s.item}>💬 WhatsApp updates</Text><Text style={s.item} onPress={()=>router.push('/support')}>🛟 Help & support ›</Text></Card><PrimaryButton title="Sign out" variant="danger" onPress={logout}/></Screen>}
const s=StyleSheet.create({avatar:{width:64,height:64,borderRadius:32,backgroundColor:colors.primarySoft,alignItems:'center',justifyContent:'center'},initial:{fontSize:28,fontWeight:'900',color:colors.primary},name:{fontSize:20,fontWeight:'900',color:colors.text},item:{paddingVertical:7,color:colors.text,fontWeight:'700'}});
