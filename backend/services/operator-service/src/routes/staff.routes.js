const r=require('express').Router(),c=require('../controllers/staff.controller');const {requireAuth}=require('../middlewares/auth.middleware');const {resolveOperator,requirePermission}=require('../middlewares/operator-context.middleware');
r.use(requireAuth,resolveOperator,requirePermission('staff.manage'));r.get('/',c.list);r.post('/',c.create);r.patch('/:id',c.update);r.post('/assignments',c.assign);module.exports=r;
